# The builder stage runs `npm install` itself; the 220MB of node_modules from
# the developer's machine is dead weight in the build context and in the layer
# that copies it.
node_modules/
dist/
.vite/
